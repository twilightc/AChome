import { BillbuttonDirective } from './billbutton.directive';

describe('BillbuttonDirective', () => {
  it('should create an instance', () => {
    const directive = new BillbuttonDirective();
    expect(directive).toBeTruthy();
  });
});
